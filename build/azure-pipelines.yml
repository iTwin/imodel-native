#Irrelevant change for Testing Linking between imodel-native and imodel-native-internal PRs
name: open-source-$(Date:yyyy.MM.dd)$(Rev:.r)

trigger:
  branches:
    include:
      - main
      - release/*
pr:
  autoCancel: true
  drafts: false
  branches:
    include:
      - main
      - release/*

resources:
  repositories:
    - repository: imodel-internal
      type: github
      name: iTwin/imodel-native-internal
      endpoint: github.com_imodel-native
      ref: Link-imodel-repos #need my branch to test for PR
      
extends:
  template: /build/templates/pipeline.yml@imodel-internal
  parameters:
    otherRepo: imodel-internal

