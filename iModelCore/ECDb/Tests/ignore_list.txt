#----------------------------------------------------------------------------------------
#   Extended tier
#----------------------------------------------------------------------------------------
#   Any fixture whose name ends in "ExtendedTests" runs on demand and on a schedule, not on
#   every developer build or PR.
#
#   Run it with:  make run-ecdb-extended
#   or directly:  ECDbTest --gtest_filter=*ExtendedTests.*
#
#   An explicit --gtest_filter on the command line replaces this whole file, which is what
#   makes that work. A test belongs here when it covers one more permutation of behaviour the
#   core tier already covers. The behaviour itself stays in the core tier.
#
*ExtendedTests.*

#----------------------------------------------------------------------------------------
#   Tests that are ignored on purpose
#----------------------------------------------------------------------------------------
ThreadSafetyTests.*     # Experimental tests for thread safety. Should not run with build.

#----------------------------------------------------------------------------------------
#   Tests that need to be looked at
#----------------------------------------------------------------------------------------

ConcurrentQueryFixture.RestartToken
ConcurrentQueryFixture.QuotaCheck
